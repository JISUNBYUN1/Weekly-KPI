# PP3G 월간ㆍ주간 분석 UI 수정본

## 적용된 변경

1. 첫 메뉴를 `월간ㆍ주간 분석`으로 변경했습니다. `대상 월`과 `대상 주차`를 함께 선택하며, 주차에서 `월간`을 선택하면 월간 분석을 표시합니다.
2. 핵심 실적에는 라이브커머스 매출, 어필리에이트 주문금액, 스마트스토어 신규 관심고객을 채널별로 분리해 표시합니다. 실제 실적 RAW 데이터 형식이 확정되면 같은 기준으로 연결할 준비가 되어 있습니다.
3. 채널별 비교 분석과 거래선별 분석을 추가했습니다. 수치, 기록 활동, 해석, 차주/차월 제안을 함께 표시합니다. 활동이 실적을 만들었다고 단정하지 않고, 같은 기간에 기록된 활동과 지표의 동행만 해석합니다.
4. 월간ㆍ주간 분석 하단에 거래선을 선택하는 `Action Item` 표를 추가했습니다.
5. `사업 전망` 메뉴를 `품목별 실적`으로 변경했습니다. 품목과 월을 고르면 채널별 실적과 1~12월 연간 추이를 확인합니다. 현재 원본에 주차별 품목 실적이 없어 주간 품목 트렌드는 RAW 파일 형식 확정 뒤 연결합니다.
6. 메뉴 클릭 시 즉시 다시 실행해 선택 메뉴의 강조 표시와 본문이 맞도록 수정했습니다.
7. `담당자 피드백` 상단에 XLSX, XLS, CSV, JSON RAW 파일 업로드 영역을 추가했습니다. 업로드 파일은 `raw_uploads` 폴더와 목록 파일에 저장됩니다.

## 적용 파일

기존 프로젝트에 다음 파일을 반영하세요.

- `streamlit_app.py` 교체
- `executive_report.py` 추가 또는 교체
- `.streamlit/config.toml` 추가. 기존 설정이 있으면 `[theme]` 내용만 병합

기존 `weekly_data.json`, `feedback.json`, 최신 운영 데이터는 덮어쓰지 마세요. ZIP에 포함된 JSON 파일은 참고용입니다.

RAW 업로드는 현재 Streamlit 배포 인스턴스의 `raw_uploads`에 저장합니다. 업로드 파일을 실제 실적 KPI에 반영하려면 파일의 열 이름, 단위, 월·주차·거래선 키를 확정한 뒤 별도 적재 로직을 추가해야 합니다.

## 검증

월간·주간 분석, 주차 월경계, 월별/주차별 빈 데이터, 품목별 실적을 포함한 8개 테스트와 기존 라이브커머스 36개 테스트가 통과했습니다. Python 문법 컴파일도 성공했습니다.

```bash
python -m pip install -r requirements.txt
python -m streamlit run streamlit_app.py
```

이 환경에서는 Streamlit 패키지를 설치할 수 없어 실제 브라우저 렌더링은 확인하지 못했습니다. 적용 후 `월간ㆍ주간 분석`의 8월 월간, 8월 W32, `품목별 실적`, `담당자 피드백` RAW 업로드를 확인해주세요.


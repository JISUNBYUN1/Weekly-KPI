# PP3G 경영진 보고 UI 수정본

첫 화면을 ‘경영진 보고’로 재구성했습니다. 네이비·화이트 중심의 색상, 통일된 글자 크기와 여백, 선택된 메뉴 강조를 적용했습니다.

## 화면 구성

1. 보고 월 선택 및 자료 기준
2. 라이브 매출·어필리에이트 주문금액·신규 관심고객 KPI
3. 거래선별 성과 표와 라이브 매출 비교 그래프
4. 보고 전 확인 사항: 자료 미제공, 음수 관심고객, 비용·주차 기준
5. 선택 월의 거래선 활동 원문 브리핑
6. 차주 보고 준비 과제 제안 및 데이터 범위

실제 원본에 근거한 지표만 표시합니다. 8월 어필리에이트 월별 자료는 없어 ‘미제공’으로 표시합니다. 마감 여부가 확인되지 않은 월간 실적을 단순 비교해 성장률이나 우수 거래선 점수를 만들지 않습니다. 기존 9개 메뉴는 유지하며 스마트스토어 메뉴의 이동 키 불일치도 수정했습니다.

활동 브리핑은 기존 `weekly_data.json`에서 해당 월의 마케팅활동·당주주요활동을 읽습니다. 제공받은 파일에는 실제 운영 `weekly_data.json`이 없어 현재는 활동 빈 상태로 표시됩니다. 연도가 없는 기존 기록은 이 앱의 기준연도인 2026년 기록으로 취급합니다. 입력 금액 단위는 혼재 가능성이 있어 이 파일의 금액을 실적 KPI에 합산하지 않습니다.

## 적용 파일

ZIP을 풀고 기존 앱 폴더에 다음을 적용합니다.

- `streamlit_app.py`: 기존 앱 파일 교체
- `executive_report.py`: 같은 폴더에 추가. 새 보고 화면과 공통 디자인을 포함하므로 반드시 함께 업로드
- `.streamlit/config.toml`: 밝은 테마와 브랜드 색상. 기존 설정이 있다면 `[theme]` 항목만 병합
- `live_commerce_data.json`: 이전에 제공된 라이브 데이터. 운영 중 업데이트한 최신 파일이 있다면 그것을 유지

참고 데이터·requirements도 ZIP에 포함했습니다. 기존 `weekly_data.json`, `feedback.json`, 최신 운영 실적을 이 묶음으로 덮어쓰지 마세요. 기존 전체 요약 화면은 새 경영진 보고로 대체되며 FCST와 프리미엄은 각각의 상세 메뉴에서 확인합니다.

이 수정은 이 대화에서 보유한 소스를 기준으로 했습니다. GitHub에 사용자가 추가 수정한 내용은 직접 확인하지 못했으므로 적용 전에 비교가 필요합니다. 배포는 수행하지 않았습니다.

## 검증

Python 문법 컴파일 및 경영진 보고 테스트 5개(12개 월 순회 포함)를 수행했습니다. 기존 라이브커머스 테스트 36개도 함께 검증했습니다. 이 환경에서는 Streamlit 패키지 설치가 되지 않아 실제 브라우저 렌더링·클릭 동작은 검증하지 못했습니다. 실행 후 경영진 보고 7월/8월/12월, 스마트스토어 메뉴, 실적 입력·수정 화면을 확인해주세요.

```bash
python -m pip install -r requirements.txt
python -m streamlit run streamlit_app.py
```

이번에는 UX/UI와 보고용 요약에 집중했습니다. 데이터베이스 영구 저장, 계정 권한 검증, 생성형 AI 평가, 액션아이템의 담당자·기한·완료 상태 저장은 추가 구현이 필요합니다.
